package main
/*
import (
	"fmt"
	"net"
	"testing"
)

func startClient() {
	conn, err := net.Dial("tcp", "localhost:8080")
	defer conn.Close()
	if err != nil {
		fmt.Println("Server not up")
	}
	conn.Write([]byte("write test 3 4\r\n"))
	content := []byte("test")
	conn.Write(content)
}

func TestMessaging(t *tesiting.T) {

}
*/